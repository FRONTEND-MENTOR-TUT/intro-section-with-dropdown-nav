import React from "react";
import IconStyled from "./style";

const Icon = ({ src, iconWidth, iconHeight }) => (
  <IconStyled src={src} iconWidth={iconWidth} iconHeight={iconHeight} />
);

export default Icon;
